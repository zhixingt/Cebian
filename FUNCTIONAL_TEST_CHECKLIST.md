# CebianX 功能测试清单

> 版本: 1.3.1
> 生成日期: 2026-06-14
> 适用范围: Chrome MV3 / Firefox MV2

---

## 目录

1. [核心聊天功能](#1-核心聊天功能)
2. [QuickActionsBar 快捷工具](#2-quickactionsbar-快捷工具)
3. [页面监控](#3-页面监控)
4. [录制与回放](#4-录制与回放)
5. [BrowserWing Skill](#5-browserwing-skill)
6. [MCP 集成](#6-mcp-集成)
7. [右键上下文菜单](#7-右键上下文菜单)
8. [设置与配置](#8-设置与配置)
9. [国际化](#9-国际化)
10. [边缘场景与回归测试](#10-边缘场景与回归测试)

---

## 1. 核心聊天功能

### 1.1 新会话创建

**功能描述**: 用户打开侧边栏后，可以创建新的聊天会话。

**前置条件**:
- 扩展已安装并启用
- 已配置有效模型（API Key 或 Web Provider 登录态正常）

**操作步骤**:
1. 点击浏览器工具栏 CebianX 图标，打开侧边栏
2. 点击顶部「新建聊天」按钮（SquarePen 图标）
3. 在输入框中输入问题并发送

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-1.1.1 | 正常创建新会话 | 打开侧边栏 → 点击新建 → 发送「你好」 | 生成新会话，URL 变为 `/chat/{sessionId}`，显示用户消息和 AI 回复 | P0 |
| TC-1.1.2 | 连续创建多个会话 | 发送第一条消息 → 点击新建 → 发送第二条消息 | 第二个会话独立，不携带第一个会话的上下文 | P1 |
| TC-1.1.3 | 未配置模型时发送 | 清除所有模型配置 → 发送消息 | 显示错误提示「请先选择一个模型」或类似引导 | P1 |
| TC-1.1.4 | 新会话自动滚动 | 发送消息后观察滚动位置 | 自动滚动到最新消息底部 | P1 |

### 1.2 消息发送与流式响应

**功能描述**: 用户输入文本（可附带附件），AI 以流式方式返回响应。

**前置条件**:
- 当前处于有效会话中
- 网络连接正常

**操作步骤**:
1. 在输入框中输入问题
2. 点击发送按钮或按 Enter
3. 观察 AI 响应过程

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-1.2.1 | 纯文本消息 | 发送「请解释量子力学」 | 收到流式文本响应，Markdown 正确渲染 | P0 |
| TC-1.2.2 | 超长文本输入 | 粘贴 5000 字文本发送 | 正常发送，响应完整，无截断 | P1 |
| TC-1.2.3 | 空消息发送 | 不输入内容直接点击发送 | 发送按钮禁用或提示输入内容 | P1 |
| TC-1.2.4 | 仅空格/换行 | 输入多个空格或换行发送 | 同 TC-1.2.3，不允许发送 | P1 |
| TC-1.2.5 | 特殊字符输入 | 输入 `<script>alert(1)</script>` | 消息正常显示，XSS 不被执行 | P0 |
| TC-1.2.6 | 网络中断 | 发送后断开网络 | 显示错误状态，提供重试按钮 | P1 |
| TC-1.2.7 | 响应中取消 | 发送后，在 AI 响应过程中点击取消 | 立即停止生成，保留已生成内容 | P1 |
| TC-1.2.8 | 重试失败消息 | 对失败消息点击重试 | 重新发送该消息，保留上下文 | P1 |

### 1.3 Markdown 渲染

**功能描述**: AI 响应中的 Markdown 内容（代码块、列表、表格等）正确渲染。

**测试用例**:

| 用例ID | 场景 | 测试输入 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-1.3.1 | 代码块 | 包含 ```python 代码块 | 语法高亮，显示复制按钮 | P1 |
| TC-1.3.2 | 表格 | 包含 Markdown 表格 | 正确渲染为 HTML 表格，对齐正常 | P1 |
| TC-1.3.3 | 嵌套列表 | 多层无序/有序列表 | 缩进正确，层级清晰 | P1 |
| TC-1.3.4 | 链接渲染 | 包含 `[链接](https://example.com)` | 可点击，新标签页打开 | P1 |
| TC-1.3.5 | 图片渲染 | 包含 `![alt](url)` | 图片正常加载，alt 文本备用 | P1 |
| TC-1.3.6 | 文本两端对齐 | 纯文本段落 | 应用 `text-align: justify`，边缘整齐 | P1 |

### 1.4 附件上传

**功能描述**: 用户可以上传图片、文件作为附件发送。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-1.4.1 | 上传图片 | 点击附件按钮 → 选择 PNG 图片 | 图片显示在输入框，发送后 AI 可分析 | P1 |
| TC-1.4.2 | 上传多个文件 | 选择 3 个文件同时上传 | 所有文件显示在输入框，可分别删除 | P1 |
| TC-1.4.3 | 超大文件 | 选择 50MB 文件 | 提示文件过大或拒绝上传 | P1 |
| TC-1.4.4 | 删除附件 | 上传后点击附件上的删除按钮 | 附件从输入框移除，不发送 | P1 |

---

## 2. QuickActionsBar 快捷工具

### 2.1 阅读页面 (read-page)

**功能描述**: 一键发送 prompt，让 AI 阅读当前页面内容并总结要点。

**前置条件**:
- 浏览器当前标签页已打开一个可访问的网页
- 页面非 `chrome://`、`about:` 等受限页面

**操作步骤**:
1. 打开目标网页（如一篇新闻文章）
2. 打开 CebianX 侧边栏
3. 点击 QuickActionsBar 上的「阅读页面」按钮（BookOpen 图标）

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-2.1.1 | 正常阅读文章页 | 打开新闻文章 → 点击阅读页面 | AI 返回页面内容总结，要点清晰 | P0 |
| TC-2.1.2 | 阅读 SPA 页面 | 打开 React/Vue 单页应用 → 点击按钮 | 能获取到动态渲染后的内容 | P1 |
| TC-2.1.3 | 阅读需要登录的页面 | 登录后打开内部系统 → 点击按钮 | 能获取到登录后的页面内容 | P1 |
| TC-2.1.4 | 阅读空页面 | 打开 `about:blank` → 点击按钮 | AI 提示页面无内容或为空 | P1 |
| TC-2.1.5 | 阅读 404 页面 | 打开不存在的 URL → 点击按钮 | AI 总结 404 页面内容（或提示页面错误） | P1 |
| TC-2.1.6 | 重复点击 | 快速连续点击两次阅读页面按钮 | 第二次被忽略（pendingTool 锁防重入） | P1 |

### 2.2 截图分析 (screenshot-analyze)

**功能描述**: 一键截取当前页面可见区域，将截图作为图片附件发送给 AI 分析。

**前置条件**:
- 当前标签页为普通网页（非受限页面）

**操作步骤**:
1. 打开目标网页
2. 打开 CebianX 侧边栏
3. 点击「截图分析」按钮（Scissors 图标）

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-2.2.1 | 正常截图分析 | 打开包含图表的页面 → 点击截图分析 | AI 收到截图附件，分析图中内容 | P0 |
| TC-2.2.2 | 截图包含复杂 UI | 打开数据仪表盘 → 点击按钮 | AI 能识别图表类型和数据趋势 | P1 |
| TC-2.2.3 | 截图权限拒绝 | 在需要特殊权限的页面截图 | 回退到纯文本 prompt「请截图当前页面并分析其中的内容」 | P1 |
| TC-2.2.4 | 长页面截图 | 打开需要滚动才能看完的页面 | 仅截取当前视口（可见区域），非完整页面 | P1 |
| TC-2.2.5 | 与阅读页面对比 | 同一页面分别使用截图分析和阅读页面 | 截图分析关注视觉内容（布局、颜色、图表），阅读页面关注文字内容 | P1 |

### 2.3 操作页面 (operate-page)

**功能描述**: 发送 prompt 让 AI 通过 `interact` 工具操作当前页面（点击、输入、滚动等）。

**前置条件**:
- 当前页面有可交互元素
- LLM 模型支持工具调用

**操作步骤**:
1. 打开目标网页
2. 点击「操作页面」按钮（MousePointerClick 图标）
3. 在聊天中告诉 AI 需要执行的操作

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-2.3.1 | 点击按钮 | 打开有按钮的页面 → 发送「点击提交按钮」 | AI 调用 interact click，按钮被点击 | P0 |
| TC-2.3.2 | 输入文本 | 打开发送表单 → 发送「在姓名框输入张三」 | AI 调用 interact type，输入框出现「张三」 | P0 |
| TC-2.3.3 | 滚动页面 | 打开长页面 → 发送「滚动到页面底部」 | 页面滚动到底部 | P1 |
| TC-2.3.4 | 选择下拉框 | 打开有 select 的页面 → 发送「选择国家为中国」 | 下拉框选中「中国」 | P1 |
| TC-2.3.5 | 操作不存在元素 | 发送「点击一个不存在的按钮」 | AI 尝试定位失败后报告错误 | P1 |
| TC-2.3.6 | 使用坐标点击 | 发送「在坐标 (100, 200) 点击」 | 在该坐标位置执行点击 | P1 |
| TC-2.3.7 | sequence 多步操作 | 发送「先输入用户名，再输入密码，然后点击登录」 | 按顺序执行 clear → type → type → click | P0 |
| TC-2.3.8 | condition 条件分支 | 页面元素可见时执行某步骤，不可见时跳过 | condition 为 visible/hidden 时正确判断 | P1 |

### 2.4 填写表单 (fill-form)

**功能描述**: 一键发送 prompt，让 AI 自动识别并填写当前页面的表单。

**前置条件**:
- 当前页面包含表单元素（input、select、textarea 等）

**操作步骤**:
1. 打开包含表单的页面
2. 点击「填写表单」按钮（FileInput 图标）
3. AI 自动分析表单结构并填写

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-2.4.1 | 简单登录表单 | 打开登录页 → 点击填写表单 | AI 识别用户名/密码框，询问或直接填写 | P0 |
| TC-2.4.2 | 复杂注册表单 | 打开注册页（含邮箱、手机、验证码等） | AI 识别各字段，按需填写 | P1 |
| TC-2.4.3 | 无表单页面 | 打开无表单的页面 → 点击按钮 | AI 提示当前页面无表单可填写 | P1 |
| TC-2.4.4 | 重复点击 | 已填写后再次点击 | 当前实现：每次点击都重新触发（待优化为首次有效） | P1 |

### 2.5 监控页面 (watch-page)

**功能描述**: 开启后，监控当前页面 DOM 变化，当检测到显著变化时通知用户。

**前置条件**:
- 当前页面为普通网页

**操作步骤**:
1. 打开目标网页
2. 点击「监控页面」按钮（Eye 图标）
3. 观察按钮状态变为激活（绿色脉冲点）
4. 在页面上执行操作触发 DOM 变化

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-2.5.1 | 开启监控 | 打开页面 → 点击监控按钮 | 按钮变绿色，显示脉冲动画 | P0 |
| TC-2.5.2 | 检测 DOM 变化 | 开启监控 → 在页面上点击按钮加载新内容 | 侧边栏显示页面变化通知，含变化数量 | P0 |
| TC-2.5.3 | 忽略微小变化 | 开启监控 → 页面出现 loading spinner | 不触发通知（loading spinner 在忽略列表中） | P1 |
| TC-2.5.4 | 关闭监控 | 再次点击监控按钮 | 按钮恢复默认状态，停止监控 | P0 |
| TC-2.5.5 | 切换标签页 | 在标签页 A 开启监控 → 切换到标签页 B | 监控仍针对标签页 A（基于注入时的 tabId） | P1 |
| TC-2.5.6 | 页面刷新 | 开启监控后刷新页面 | 监控状态丢失，需重新开启 | P1 |
| TC-2.5.7 | 冷却期机制 | 触发一次通知后，8 秒内再次变化 | 8 秒内变化累计，8 秒后统一通知；30 秒冷却期内不重复通知 | P1 |

---

## 3. 页面监控

### 3.1 MutationObserver 行为

**功能描述**: 通过 `startPageWatcher` 注入页面的 MutationObserver 检测 DOM 变化。

**核心参数**:
- 防抖延迟: 8000ms
- 冷却期: 30000ms
- 变化阈值: 12 个 significant changes

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-3.1.1 | 检测新增元素 | 开启监控 → 页面上动态插入 15 个 div | 触发 `page_change_detected` 消息 | P0 |
| TC-3.1.2 | 忽略 script/style | 开启监控 → 页面动态插入 `<script>` | 不触发通知（IGNORED_TAGS 过滤） | P1 |
| TC-3.1.3 | 忽略广告类元素 | 插入 class="ad-banner" 的元素 | 不触发通知（IGNORED_PATTERNS 匹配） | P1 |
| TC-3.1.4 | 文本内容变化 | 修改已有元素的 textContent | 触发通知（characterData 类型） | P1 |
| TC-3.1.5 | 低于阈值 | 仅插入 5 个 div | 不触发通知（< 12） | P1 |
| TC-3.1.6 | 多实例保护 | 连续两次调用 startPageWatcher | 第二次返回 `already_watching` | P1 |
| TC-3.1.7 | 停止监控 | 调用 stopPageWatcher | observer disconnect，状态清除 | P0 |

---

## 4. 录制与回放

### 4.1 开始录制

**功能描述**: 用户点击录制按钮，开始记录当前页面的用户操作（点击、输入、滚动等）。

**前置条件**:
- 当前页面为普通网页
- 无其他实例正在录制

**操作步骤**:
1. 打开目标网页
2. 点击工具栏的录制按钮（CircleDot 图标）
3. 在页面上执行操作

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-4.1.1 | 正常开始录制 | 打开页面 → 点击录制按钮 | 按钮变为红色脉冲状态，开始记录 | P0 |
| TC-4.1.2 | 多实例冲突 | 在窗口 A 开始录制 → 在窗口 B 点击录制 | 窗口 B 显示「已有其他实例正在录制」提示 | P1 |
| TC-4.1.3 | 无权限页面 | 在 `chrome://settings` 点击录制 | 录制可能无法注入，提示不支持 | P1 |

### 4.2 停止录制

**功能描述**: 用户点击停止按钮，结束录制并生成录制附件。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-4.2.1 | 手动停止 | 录制中 → 点击停止按钮 | 录制结束，聊天记录中出现录制附件 chip | P0 |
| TC-4.2.2 | 发送时自动停止 | 录制中 → 发送消息 | 录制自动停止，录制内容作为附件发送 | P1 |
| TC-4.2.3 | cap-trigger 停止 | 录制达到事件上限或时间上限 | 自动停止，显示原因提示（事件数限制/时间限制） | P1 |
| TC-4.2.4 | 重复点击停止 | 快速点击两次停止 | 第二次复用第一次的 Promise，不重复发送 stop | P1 |

### 4.3 录制编辑器

**功能描述**: 点击录制附件后打开编辑器，可查看、修改、删除、排序步骤，并保存为快捷指令。

**操作步骤**:
1. 点击聊天记录中的录制附件
2. 在编辑器中查看步骤列表
3. 修改/删除/插入步骤
4. 点击回放或保存

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-4.3.1 | 查看步骤 | 点击录制附件 | 弹出 Dialog，显示所有步骤（action、selector、value） | P0 |
| TC-4.3.2 | 修改步骤值 | 点击某步骤的 value 输入框 → 修改内容 | 该步骤值更新，不影响其他步骤 | P1 |
| TC-4.3.3 | 修改 condition | 将某步骤 condition 设为 visible | 保存后该步骤仅在元素可见时执行 | P1 |
| TC-4.3.4 | 删除步骤 | 点击步骤旁的删除按钮 | 步骤从列表移除，剩余步骤顺序正确 | P1 |
| TC-4.3.5 | 插入步骤 | 点击「插入步骤」按钮 | 在指定位置插入默认 click 步骤 | P1 |
| TC-4.3.6 | 上移/下移 | 点击步骤的上移/下移箭头 | 步骤位置交换，localId 不变 | P1 |
| TC-4.3.7 | 排序后 key 稳定 | 上移下移后检查 React key | 不发生组件状态错乱（使用 localId 而非 index 作为 key） | P1 |
| TC-4.3.8 | 回放步骤 | 点击回放按钮 | 关闭编辑器，通过 interact sequence 执行步骤 | P0 |
| TC-4.3.9 | 保存为快捷指令 | 输入名称和描述 → 点击保存 | 在 `~/.cebian/prompts/` 下生成 `.md` 文件，toast 提示成功 | P1 |
| TC-4.3.10 | 保存文件名安全 | 输入名称「测试/文件<>」 | 特殊字符被替换为 `-`，首尾 `-` 被 trim | P1 |
| TC-4.3.11 | 空步骤回放 | 删除所有步骤后点击回放 | 不执行任何操作或提示无步骤 | P1 |

### 4.4 录制内容回放

**功能描述**: 通过 interact 工具的 `sequence` action 回放录制的操作步骤。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-4.4.1 | 正常回放 | 录制登录流程 → 回放 | 页面按录制顺序执行点击、输入、提交 | P0 |
| TC-4.4.2 | 页面结构变化后回放 | 录制后页面 DOM 结构改变 | 元素定位失败，报错并停止后续步骤 | P1 |
| TC-4.4.3 | SPA 导航后回放 | 录制包含页面跳转的流程 | scroll delta 基准值正确重置，不累积异常 | P1 |
| TC-4.4.4 | 条件步骤回放 | 步骤含 condition: visible | 元素不可见时跳过该步骤，继续后续 | P1 |
| TC-4.4.5 | 超时处理 | 步骤设置 timeout: 1000，元素 2 秒后才出现 | 超时报错，停止执行 | P1 |

### 4.5 录制导出

**功能描述**: 将录制会话导出为 attachment，支持多种格式。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-4.5.1 | 导出为附件 | 停止录制 | 录制内容作为 `recording` 类型附件出现在输入框 | P0 |
| TC-4.5.2 | 导出文件完整性 | 检查附件 JSON 内容 | 包含完整的 RecordedSession（events、tabs、startTime 等） | P1 |

---

## 5. BrowserWing Skill

### 5.1 Skill 部署

**功能描述**: CebianX 启动时自动将内置 BrowserWing skill 部署到 VFS。

**前置条件**:
- BrowserWing 本地服务已启动（`http://127.0.0.1:8080`）

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-5.1.1 | 首次部署 | 安装扩展后首次打开 | `~/.cebian/skills/browserwing/` 下出现 SKILL.md 和 scripts/ | P0 |
| TC-5.1.2 | 幂等部署 | 重启扩展/浏览器 | 已存在的 skill 文件不被覆盖 | P1 |
| TC-5.1.3 | 用户修改保护 | 用户修改 skill 文件后重启 | 用户修改保留，不被内置版本覆盖 | P1 |

### 5.2 API 调用 (scripts/api.js)

**功能描述**: 通过 run_skill 调用 BrowserWing 的 32 个 action。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-5.2.1 | navigate | 调用 action: navigate, url: "https://example.com" | BrowserWing 打开该 URL | P0 |
| TC-5.2.2 | snapshot | 调用 action: snapshot | 返回页面 accessibility tree 和 RefIDs | P0 |
| TC-5.2.3 | click by RefID | 调用 action: click, identifier: "@e1" | 点击对应元素 | P0 |
| TC-5.2.4 | type | 调用 action: type, identifier: "@e2", text: "hello" | 在对应输入框输入文本 | P0 |
| TC-5.2.5 | batch | 调用 action: batch, operations: [...] | 按顺序执行多个操作 | P0 |
| TC-5.2.6 | fillForm | 调用 action: fillForm, url, data | 智能识别并填写表单 | P0 |
| TC-5.2.7 | screenshot | 调用 action: screenshot, fullPage: true | 返回 base64 截图数据 | P1 |
| TC-5.2.8 | evaluate | 调用 action: evaluate, expression: "document.title" | 返回 JavaScript 执行结果 | P1 |
| TC-5.2.9 | 错误处理 | 调用不存在的 action: "foo" | 抛出错误，提示支持的 action 列表 | P1 |
| TC-5.2.10 | 网络错误 | BrowserWing 未启动时调用 | 抛出 `BrowserWing /navigate failed: HTTP 0` 或连接错误 | P1 |

### 5.3 批量表单填写 (scripts/batch-fill.js)

**功能描述**: 循环读取数据数组，逐条打开页面并填写表单。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-5.3.1 | 正常批量填写 | 提供 3 条数据，调用 batch-fill | 逐条填写并提交，返回 success: 3 | P0 |
| TC-5.3.2 | 截图留痕 | 设置 screenshot: true | 每条填写后截图，结果中包含 screenshotUrl | P1 |
| TC-5.3.3 | 提交失败回退 | 某条数据提交后页面显示错误 | 记录失败原因，继续处理下一条 | P1 |
| TC-5.3.4 | 空数据 | entries 为空数组 | 返回 total: 0, success: 0, failed: 0 | P1 |
| TC-5.3.5 | 人工确认 | 设置 confirm: true | 每条填写后暂停，等待用户确认后再提交 | P1 |
| TC-5.3.6 | 自定义提交按钮 | 设置 submitSelector: "@e5" | 点击指定 RefID 的元素提交 | P1 |

---

## 6. MCP 集成

### 6.1 MCP Server 配置

**功能描述**: 在设置中添加、编辑、删除 MCP Server。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-6.1.1 | 添加 HTTP MCP | 输入名称、URL `http://localhost:3456/mcp`、选择 streamable-http | 保存成功，状态显示已连接 | P0 |
| TC-6.1.2 | 添加 SSE MCP | 输入名称、URL `http://localhost:3456/sse`、选择 sse | 保存成功 | P0 |
| TC-6.1.3 | 添加带认证 MCP | 选择 Bearer Token，输入 token | 请求头中包含 Authorization: Bearer xxx | P1 |
| TC-6.1.4 | 连接失败 | 输入未启动的服务 URL | 状态显示连接失败或超时 | P1 |
| TC-6.1.5 | 删除 MCP | 点击删除按钮 | 该 MCP 从列表移除，LLM 不再调用其工具 | P1 |
| TC-6.1.6 | 工具自动发现 | 连接 BrowserWing MCP | LLM 自动获得 navigate、click、type 等工具 | P0 |

### 6.2 MCP 工具调用

**功能描述**: LLM 通过 MCP 协议调用外部工具。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-6.2.1 | 正常调用 | 发送「打开 example.com」 | LLM 调用 MCP navigate 工具，页面跳转 | P0 |
| TC-6.2.2 | 限流保护 | 快速连续发送 10 条需要 MCP 的消息 | 受 ServerThrottle 限制，不会压垮服务 | P1 |
| TC-6.2.3 | 断路器 | MCP 服务连续失败 5 次 | 断路器打开，后续请求直接失败，不再访问服务 | P1 |
| TC-6.2.4 | 自定义 Header | 在 MCP 配置中添加 `X-Custom: value` | 每次请求都携带该 header | P1 |

---

## 7. 右键上下文菜单

### 7.1 菜单项显示

**功能描述**: 在页面上右键点击，显示 CebianX 相关菜单项。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-7.1.1 | 页面右键 | 在空白处右键 | 显示「用 Cebian 阅读此页面」「用 Cebian 总结此页面」「用 Cebian 截图此页面」 | P0 |
| TC-7.1.2 | 选中文本右键 | 选中一段文字后右键 | 额外显示「用 Cebian 操作选中内容」 | P0 |
| TC-7.1.3 | 链接右键 | 在链接上右键 | 额外显示「用 Cebian 打开此链接」 | P0 |
| TC-7.1.4 | 图片右键 | 在图片上右键 | 不显示 CebianX 专属菜单项（当前未实现） | P1 |

### 7.2 菜单项点击

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-7.2.1 | 阅读此页面 | 点击「用 Cebian 阅读此页面」 | 侧边栏打开，自动发送「请阅读当前页面的内容...」 | P0 |
| TC-7.2.2 | 总结此页面 | 点击「用 Cebian 总结此页面」 | 自动发送「请总结当前页面的主要内容...」 | P0 |
| TC-7.2.3 | 操作选中内容 | 选中「人工智能」→ 点击菜单项 | 自动发送「请对当前选中的内容进行操作："人工智能"」 | P0 |
| TC-7.2.4 | 打开此链接 | 在链接上右键 → 点击菜单项 | 自动发送「请在当前页面中点击或打开这个链接：https://...」 | P0 |
| TC-7.2.5 | 截图此页面 | 点击「用 Cebian 截图此页面」 | 自动发送「请截取当前页面的截图」 | P0 |
| TC-7.2.6 | 侧边栏未打开 | 关闭侧边栏 → 点击右键菜单 | 侧边栏自动打开，然后发送 prompt | P1 |

---

## 8. 设置与配置

### 8.1 模型选择

**功能描述**: 在设置中选择和配置 AI 模型。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-8.1.1 | 选择 API Key 模型 | 选择 GPT-4 → 输入 API Key | 保存后可以使用该模型聊天 | P0 |
| TC-8.1.2 | 选择 Web Provider | 选择 GLM → 点击登录 → 在弹出页登录 | 登录成功后显示「已登录」，无需 API Key | P0 |
| TC-8.1.3 | Web Provider 登录态过期 | 使用已过期会话发送消息 | 提示需要重新登录，提供一键重登入口 | P1 |
| TC-8.1.4 | 切换模型 | 从 GPT-4 切换到 Kimi | 新消息使用 Kimi 模型，历史消息保留 | P1 |
| TC-8.1.5 | 无效 API Key | 输入错误格式的 Key | 发送时提示认证失败 | P1 |

### 8.2 MCP 设置

参见 [6. MCP 集成](#6-mcp-集成)。

### 8.3 侧边栏折叠

**功能描述**: 点击页面上的悬浮按钮，展开/折叠侧边栏。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-8.3.1 | 展开侧边栏 | 点击页面上的 CebianX 悬浮按钮 | 侧边栏从右侧滑出 | P1 |
| TC-8.3.2 | 折叠侧边栏 | 再次点击悬浮按钮 | 侧边栏收起 | P1 |
| TC-8.3.3 | 多标签页同步 | 在标签页 A 展开 → 切换到标签页 B | 标签页 B 也显示悬浮按钮 | P1 |

---

## 9. 国际化

### 9.1 语言切换

**功能描述**: 支持简体中文、繁体中文、英文三种语言。

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-9.1.1 | 切换为英文 | 设置中切换语言为 English | 所有 UI 文本变为英文 | P1 |
| TC-9.1.2 | 切换为繁体中文 | 切换为繁体中文 | 所有 UI 文本变为繁体 | P1 |
| TC-9.1.3 | key 缺失回退 | 删除某个语言文件中的一个 key | 显示英文（fallback）或 key 本身 | P1 |
| TC-9.1.4 | 允许列表检查 | 添加不在 ALLOWLIST 中的顶层 key | `pnpm run check` 报错 | P1 |

---

## 10. 边缘场景与回归测试

### 10.1 Service Worker 生命周期

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-10.1.1 | SW 空闲后唤醒 | 长时间不操作 → 发送消息 | SW 被唤醒，消息正常处理 | P1 |
| TC-10.1.2 | SW 崩溃重启 | 强制终止 SW → 发送消息 | SW 自动重启，状态恢复 | P1 |
| TC-10.1.3 | 端口断开重连 | 关闭侧边栏 → 重新打开 | 自动重连 port，恢复会话状态 | P1 |

### 10.2 数据持久化

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-10.2.1 | 会话持久化 | 发送多条消息 → 关闭浏览器 → 重新打开 | 历史消息完整保留 | P0 |
| TC-10.2.2 | IndexedDB 迁移 | 升级扩展版本 | 旧数据自动迁移，不丢失 | P1 |
| TC-10.2.3 | 存储配额满 | 发送大量消息使存储接近上限 | 优雅处理，提示用户清理 | P1 |

### 10.3 跨浏览器兼容

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-10.3.1 | Chrome 运行 | 在 Chrome 安装扩展并测试核心功能 | 所有功能正常 | P0 |
| TC-10.3.2 | Firefox 运行 | 在 Firefox 安装扩展并测试 | 核心功能正常（MV2 适配） | P0 |
| TC-10.3.3 | 不同分辨率 | 在 1920x1080 和 1366x768 下使用 | UI 自适应，无错位 | P1 |

### 10.4 安全性

**测试用例**:

| 用例ID | 场景 | 操作步骤 | 预期结果 | 优先级 |
|--------|------|---------|---------|--------|
| TC-10.4.1 | XSS 防护 | 发送包含 `<script>alert(1)</script>` 的消息 | 脚本不执行，纯文本显示 | P0 |
| TC-10.4.2 | CSP 合规 | 检查所有内联脚本 | 无不安全的 `eval` 或内联事件 | P1 |
| TC-10.4.3 | 敏感信息不泄露 | 检查日志和网络请求 | API Key 不出现在日志中 | P0 |

---

## 附录 A：测试环境准备

### A.1 必要工具

- Chrome 120+ 或 Firefox 115+
- Node.js 20+
- BrowserWing 本地服务（如需测试 skill）
- Playwright MCP Server（如需测试 MCP）

### A.2 测试页面

准备以下测试页面：
1. **静态文章页**: `https://example.com` 或本地 HTML（测试阅读页面）
2. **表单页**: 包含 input、select、button 的本地 HTML（测试填写表单、操作页面）
3. **SPA 页**: React/Vue 示例应用（测试 SPA 导航、DOM 变化）
4. **图表页**: 包含 ECharts/D3 图表的页面（测试截图分析）
5. **登录页**: 模拟登录表单（测试录制回放）

### A.3 快速验证脚本

```bash
# 1. 启动测试服务
cd cebian-web-provider
pnpm dev

# 2. 运行单元测试
pnpm test

# 3. 运行 E2E 测试
pnpm e2e

# 4. 启动 BrowserWing（如测试 skill）
npx @executeautomation/playwright-mcp-server --port 8080
```

---

## 附录 B：问题记录模板

发现 bug 时按以下格式记录：

```
- 用例ID: TC-x.x.x
- 问题描述: 
- 复现步骤:
  1. 
  2. 
  3. 
- 预期结果:
- 实际结果:
- 环境: Chrome/Firefox + 版本
- 截图/日志:
- 严重程度: 阻塞/严重/一般/轻微
```
