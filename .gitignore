# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts
.worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment overrides (may contain dev API keys, etc.)
# `.env` / `.env.example` are committed; `*.local` are personal overrides.
.env.local
.env.*.local

# Astro site build output / cache
site/.astro/
site/dist/

# Private notes folder, never committed
docs/
# Exception: superpowers design specs are committed artifacts, not private notes
!docs/superpowers/
!docs/superpowers/**
