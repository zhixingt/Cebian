# web-browser-cookie-extraction

Real cookie extraction via chrome.cookies.getAll + AES-GCM 256 encrypted storage + 6 production-grade enhancements (A1 UI transparency, A2 configurable session indicators [KEY], A3 active tab tracking, A4 audit log, A5 refresh retry, A6 detect open tab)
